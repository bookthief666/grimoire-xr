# Temporary guarded retirement trigger

This branch exists only to trigger the one-shot legacy Sanctum retirement workflow from a same-repository pull request whose base already contains the workflow definition. It must not be merged.
