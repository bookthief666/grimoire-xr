# 0.48 Cross-Client Conformance & Doctrine De-duplication

Planning-only until 0.47 freeze SHA is pushed.

Preconditions: exact Fold-tested 0.47 runtime is pushed; branch is cut from that SHA; VR semantic authority remains pinned to `tarot-archetype-vr@f4534b4f92d88f3950ec0c9c211bfa4648cd08ea` (`grimoire.tarot.semantic.v1@1.0.0`).

Goal: prove executable semantic parity between Fold and VR before any shared-package extraction.

Mandatory parity: all 78 canonical IDs and legacy indices; reviewed Thoth expression/correspondence source claims; spread topology; relation methods; Three-Aces fixture; Major-gap `UNSPECIFIED / CARD_WITHOUT_SUIT_FAMILY`; 0.47 orthogonal axes remaining downstream of canonical truth; source-honest fresh RWS; explicit RWS + Crowley relation provenance without RWS correspondence fiction; no `thoth_native` relation algorithm; loud contract-pin drift failure.

Phases: normalized snapshots -> deterministic parity fixtures -> 0.47 axis conformance -> duplicated-doctrine inventory -> only then decide whether a shared pure package reduces risk.

Explicit exclusions: React/UI, persistence, providers, Fold layout, VR geometry/input/haptics, generated prose/art, visual current/enchantment.