# 0.47 Freeze Handoff

0.47 is visually and behaviorally accepted on Samsung Galaxy Z Fold 6. The remaining release-management action is to commit and push the exact local six-file runtime candidate.

## Accepted runtime delta

Exactly these tracked files must be modified before the freeze commit:

- `src/App.jsx`
- `src/reliquary/reliquaryStore.js`
- `src/tarotBridge/archiveEnvelope.js`
- `src/tarotBridge/canonicalTarotBridge.js`
- `src/tarotBridge/oracleSynthesis.js`
- `src/tarotBridge/thresholdReading.js`

`.vercel/` must remain untracked and unstaged.

## Required evidence

Before staging:

- Phase A ontology QA passes;
- Phase B bridge QA and integration verification pass;
- Phase C runtime QA and integration verification pass;
- Phase D Codex UX QA and integration verification pass;
- `git diff --check` passes;
- full Vitest suite and production build pass;
- `npm run qa:ontology:freeze` reports `0.47 exact Fold freeze candidate: PASS`.

## Freeze commit

Stage only the six runtime files, run staged diff safety, then commit with:

`feat(app): freeze 0.47 semantic doctrine and Fold Codex runtime`

Push only `agent/0.47-semantic-ontology-integrity`.

Do not merge or deploy as part of the freeze.

## After push

Record the exact pushed SHA in PR #54 and issue #53. Only then cut the 0.48 conformance branch from that exact SHA.
