# 0.48 D3 — Relation Authority De-duplication

## Incoming evidence

D2 reached zero drift across:

- 50 ordered pair/context cases;
- 125 representative three-card family combinations;
- relation types and reason codes;
- source IDs and claim IDs;
- kernel/source-pack versions;
- outer-pair and center-context effects;
- ReadingRecord relation provenance.

D1 is frozen at `009627edc76ea57b381e2efd02ac6399b1224602` and enforces the Tarot-system compatibility firewall.

## D3 objective

Remove the independently maintained Liber LXXVIII doctrine from the Fold runtime without changing reading behavior.

The authoritative VR producer exports a compact relation-authority snapshot directly from production modules:

- relation-method registry + selection provenance;
- dignity kernel version;
- Liber LXXVIII source-pack version/source ID;
- 50 normalized pair/context facts;
- 25 normalized center-context rules.

The Fold activator validates the exact `grimoire.tarot.semantic.v1@1.0.0` contract pinned to `f4534b4f92d88f3950ec0c9c211bfa4648cd08ea`, generates a presentation-free authority artifact, and rewires only generic relation lookup/binding mechanics to consume it.

## Doctrine removed from the Fold bridge

D3 must remove local copies of:

- the explicit five-pair relation table;
- adjacency claim ID;
- same-suit-strengthening claim ID;
- center-between-contraries claim ID;
- Liber LXXVIII source ID constant;
- Book of Thoth method-source ID constant;
- Thoth method-inheritance claim ID constant.

The Fold may retain generic mechanics: position binding, ReadingRecord construction, relation lookup, provenance aggregation, and semantic authorization.

## Required post-activation proof

1. D3 verifier passes.
2. D2 exhaustive 50 + 125 comparison remains zero drift.
3. Existing cross-client semantic parity core remains zero drift.
4. Full Fold tests and production build pass.
5. Candidate contains only `src/tarotBridge/canonicalTarotBridge.js` plus generated `src/tarotBridge/authoritativeRelationAuthority.generated.js` (and ignored local `.vercel/`).

No UI, persistence, providers, source facts, VR runtime, or presentation behavior changes in D3.
